anthropic>=0.25.0
discord.py>=2.3.0
httpx>=0.27.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
pytz>=2024.1
# aiohttp hosts the Strava + WHOOP webhook receiver (co-located in the same
# event loop as the Discord bot; see integrations/webhook_server.py).
aiohttp>=3.9.0
# FastAPI service (api_server.py) — backend for the Coach Aurelius iOS app.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# Crash reporting — errors from the bot and the FastAPI server go to Sentry,
# which forwards them to Discord (#fitness-bot in the Sentry Alerts server).
sentry-sdk>=2.0
